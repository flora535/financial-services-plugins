---
description: Assess whether Bitcoin is near a cycle bottom
argument-hint: "[current price or 'latest']"
---

Load the `btc-bottom` skill and run a bottom-call analysis.

If current price provided, use it. Otherwise fetch live via MCP.
