---
description: Assess whether Bitcoin is near a cycle top
argument-hint: "[current price or 'latest']"
---

Load the `btc-top` skill and run a top-risk analysis.

If current price provided, use it. Otherwise fetch live via MCP.
